<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login with Magic Link</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f9f9f9;
            color: #333;
            padding: 1.25rem;
        }
        .logo {
            text-align: center;
            margin-bottom: 1.25rem;
        }
        .logo img {
            max-width: 10rem;
            height: auto;
        }
        .container {
            background: #ffffff;
            padding: 2rem;
            max-width: 40rem;
            margin: auto;
            border-radius: 0.5rem;
            box-shadow: 0 0 0.5rem rgba(0,0,0,0.1);
        }
        a.button {
            display: inline-block;
            padding: 0.75rem 1.25rem;
            background: #007bff;
            color: #ffffff;
            text-decoration: none;
            border-radius: 0.5rem;
            margin-top: 1.25rem;
        }
        .recipient-note {
            text-align: center;
            margin-top: 1.5rem;
            font-size: 0.85em;
            color: #aaa;
        }
    </style>
</head>
<body>

    {% if logo_url %}
    <div class="logo">
        <img src="{{ logo_url }}" alt="Logo">
    </div>
    {% endif %}

    <div class="container">
        <h2>Login Instantly</h2>

        <p>Hello {{ display_name | default(username) | default("there") }},</p>

        <p>You requested to log in using a magic link.</p>

        <p>Click the button below to securely log in to your account:</p>

        <p>
            <a href="{{ callback_url }}" class="button">Log In with Magic Link</a>
        </p>

        <p>This link will expire shortly. If you didn’t request it, you can safely ignore this message.</p>
    </div>

    {% if recipient_email %}
    <div class="recipient-note">
        This email was sent to <a href="mailto:{{ recipient_email }}">{{ recipient_email }}</a>.
    </div>
    {% endif %}

</body>
</html>
